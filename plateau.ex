defmodule Plateau do
  defstruct [:coord]
  # 
  # def within_boundaries(rover, plateau) when rover.coord.x_coord >=0 and rover.coord.y_coord >= 0 and rover.coord.x_coord <=plateau.coord.x_coord and rover.coord.y_coord <= plateau.coord.y_coord do
  #   IO.puts 'The rover is within boundaries'
  # end
  #
  # def within_boundaries(rover, plateau) do
  #   IO.puts 'The rover is out of the plateau'
  # end
end
